{
  "name": "akara",
  "version": "0.1.0",
  "private": true,
  "description": "WhatsApp-first peer-to-peer FX matching assistant.",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "test": "node server/test/run-tests.js && node server/test/verification-test.js",
    "test:verification": "node server/test/verification-test.js",
    "check": "node --check server/index.js && node --check server/app.js && node --check server/website.js && node --check server/lib/listing-card.js"
  },
  "dependencies": {
    "opentype.js": "^2.0.0",
    "sharp": "^0.33.5"
  }
}
